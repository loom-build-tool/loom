apply plugin: 'java'

archivesBaseName = 'loom-plugin-junit4'

dependencies {
    compileOnly project(':api')
    compileOnly project(':util')
    compileOnly('junit:junit:4.12')
}

jar {
    manifest {
        attributes(
            'Class-Path': configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}

