apply plugin: 'java'

dependencies {
    compileOnly project(':api')
    compileOnly project(':util')
    compile('org.ow2.asm:asm:6.0_BETA')
}

jar {
    manifest {
        attributes(
            'Class-Path': configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}

